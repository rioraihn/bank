# Wallet Service Environment Configuration
# Copy this file to .env and update the values as needed

# Server Configuration
# Host for the HTTP server to bind to
# Use "localhost" for local development only
# Use "0.0.0.0" to bind to all network interfaces (for production/Docker)
SERVER_HOST=localhost

# Port for the HTTP server
SERVER_PORT=8080

# Logging Configuration
# Set to "true" to enable debug logging with file and line numbers
DEBUG=false

# Notes:
# - The service starts with empty memory repositories (no pre-populated data)
# - Create wallets and perform transactions via API endpoints
# - All data is stored in memory and lost on restart